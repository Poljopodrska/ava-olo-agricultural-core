version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install -r requirements.txt
run:
  runtime-version: 3.8
  command: python simple_test_app.py
  network:
    port: 8080
    env: PORT